using Microsoft.Xna.Framework.Input;

namespace main
{
    class Controls 
    {
        private int speed;
        if (Keyboard.GetState().IsKeyDown(Keys.W)
        {
            speed += 1;
        }
    }
}