using Microsoft.Xna.Framework;
namespace Camera
{
    public class Camera
    {
        private Rectangle scope;

        public Camera(Rectangle scope)
        {
            this.scope = scope;
        }
    }
}